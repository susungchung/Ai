#include "Game.h"

ModeManager::ModeManager() 
{
	mode_type = MODE1;
}
ModeManager::~ModeManager() 
{
}
Player* ModeManager::get_player_one() {
	switch (mode_type) {
	case MODE1:
		
	};
}
Player* ModeManager::get_player_two() {
	switch (mode_type) {
	case MODE1:

	};
}